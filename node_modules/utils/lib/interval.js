(function () {
	/*need for handlemort only*/
	"use strict";
	module.exports = function interval(arr, from, to) {
		return arr.filter(function (o) {return to === undefined ? o.x >= from :  o.x >= from && o.x <= to;});
	}
})()